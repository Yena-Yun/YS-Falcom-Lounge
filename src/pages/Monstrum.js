import React from "react";
import styled from "styled-components";

const Monstrum = (props) => {
  return <MonstrumBlock></MonstrumBlock>;
};

const MonstrumBlock = styled.div``;

export default Monstrum;
